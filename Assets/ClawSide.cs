using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClawSide : MonoBehaviour
{

    //private void OnTriggerEnter(Collider collision)
    //{
    //    ClawControl go = this.GetComponentInParent<ClawControl>();
    //    go.GamePieceHit(collision.gameObject);
    //    //Debug.Log($"{this.name} in {go.name} triggered by {collision.gameObject.name}");

    //}
    ////Detect collisions between the GameObjects with Colliders attached
    //void OnCollisionEnter(Collision collision) {
    //    ClawControl go = this.GetComponentInParent<ClawControl>();
    //    go.GamePieceHit(collision.gameObject);
    //    //Debug.Log($"{this.name} in {go.name} collided with {collision.gameObject.name}");
    //}

}
